#include "ft_printf.h"
#include "libft.h"
#include "op.h"
#include "generic_list.h"
#include "tokens.h"
#include "function_prototypes.h"



char *get_label(char *line, t_line_token *token)
{
    ;
}

t_line_token *new_line_token(char *line)
{
    t_line_token *token;

    token = mallokill(sizeof(t_line_token));
    token->index = 0;

}

int main()
{
    return (0);
}